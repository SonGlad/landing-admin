export const selectContacts = state => state.data.contacts;

export const selectContactsByResource = state => state.data.contactsByResource;

export const selectLoading = state => state.data.isLoading;

export const selectFilter = state => state.data.filter;

export const selectCheckbox = state => state.data.selectedCheckedCheckbox;